naftiko: 1.0.0-alpha2
info:
  label: Amazon MemoryDB — NodeTypes
  description: 'Amazon MemoryDB — NodeTypes. 1 operations. Lead operation: Amazon MemoryDB ListAllowedNodeTypeUpdates. Self-contained
    Naftiko capability covering one Amazon Memorydb business surface.'
  tags:
  - Amazon Memorydb
  - NodeTypes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_MEMORYDB_API_KEY: AMAZON_MEMORYDB_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-memorydb-nodetypes
    baseUri: https://memory-db.us-east-1.amazonaws.com
    description: Amazon MemoryDB — NodeTypes business capability. Self-contained, no shared references.
    resources:
    - name: nodetypes
      path: /nodetypes
      operations:
      - name: listallowednodetypeupdates
        method: GET
        description: Amazon MemoryDB ListAllowedNodeTypeUpdates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_MEMORYDB_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-memorydb-nodetypes-rest
    port: 8080
    description: REST adapter for Amazon MemoryDB — NodeTypes. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/nodetypes
      name: nodetypes
      description: REST surface for nodetypes.
      operations:
      - method: GET
        name: listallowednodetypeupdates
        description: Amazon MemoryDB ListAllowedNodeTypeUpdates
        call: amazon-memorydb-nodetypes.listallowednodetypeupdates
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-memorydb-nodetypes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon MemoryDB — NodeTypes. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: amazon-memorydb-listallowednodetypeupdates
      description: Amazon MemoryDB ListAllowedNodeTypeUpdates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: amazon-memorydb-nodetypes.listallowednodetypeupdates
      outputParameters:
      - type: object
        mapping: $.
